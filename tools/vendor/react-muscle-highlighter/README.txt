Vendored from https://github.com/soroojshehryar/react-muscle-highlighter (MIT license).
Backup so data/bodymap_src.json can always be rebuilt (see ../gen_bodymap_src.py) — the
prior copy of that file went missing from the repo with no source to regenerate it from.

Files:
  bodyFront.ts, bodyBack.ts   — assets/bodyFront.ts, assets/bodyBack.ts (muscle slug -> SVG paths)
  SvgMaleWrapper.tsx          — components/SvgMaleWrapper.tsx (body outline paths + viewBox)

Not modified from upstream.
